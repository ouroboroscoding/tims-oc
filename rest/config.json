{
	"developer": {
		"emails": ["admin@localhost"]
	},

	"email": {
		"from": "admin@localhost",
		"smtp": {
			"host": "localhost",
			"port": 587,
			"tls": true,
			"user": "noone",
			"passwd": "nopasswd"
		}
	},

	"mysql": {
		"hosts": {
			"primary": {
				"host": "localhost",
				"port": 3306,
				"charset": "utf8",
				"user": "",
				"passwd": ""
			}
		}
	},

	"redis": {
		"session": {
			"host": "localhost",
			"port": 6379,
			"db": 0,
			"charset": "utf8"
		}
	},

	"rest": {
		"allowed": "localhost",
		"default": {
			"domain": "localhost",
			"host": "0.0.0.0",
			"port": 8600,
			"protocol": "http",
			"workers": 1
		},
		"services": {
			"primary": {"port": 0}
		}
	},

	"s3": {
		"expires": 86400,
		"profile": "s3",
		"conf": {
			"connect_timeout": 5,
			"read_timeout": 2
		},
		"bucket": "",
		"path": ""
	},

	"services": {
		"salt": null
	}
}
